DATABASE_URL=file:./data/bpmn.db
# Neon later: DATABASE_URL=postgresql://...
DB_PROVIDER=sqlite
PORT=5173

# Google OAuth (server-only; never expose with a VITE_ prefix)
# Google Cloud Console → APIs & Services → Credentials → Create OAuth client ID
#   Application type: Web application
#   Authorized JavaScript origins: http://localhost:5173
#   Authorized redirect URIs:     http://localhost:5173/api/auth/google/callback
# Production: same paths on your public origin, e.g. https://example.com/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SESSION_SECRET=
# Optional override if the request Host is wrong behind a proxy:
# AUTH_BASE_URL=http://localhost:5173

# AI (names from Trigger Chains / Лотос; fill locally, do not commit secrets)
AI_PROVIDER=nvidia
NVIDIA_API_KEY=
NVIDIA_MODEL=meta/llama-3.3-70b-instruct
LLM_MODEL=meta/llama-3.3-70b-instruct
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-pro-preview
